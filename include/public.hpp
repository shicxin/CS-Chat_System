// 公共文件
#ifndef PUBLIC_H
#define PUBLIC_H

// #include <
/// @brief 
enum enMsgType {
    LOGIN_MSG = 1, // 登录消息
    LOGIN_MSG_ACK, // 登录响应消息
    REG_MSG, // 注册消息
    REG_MSG_ACK,// 注册响应消息
    MSG_NUMBER // 消息数量
};

#endif

// LOG_INFO << "\n\n1\n\n";